---
ansible-navigator:
  execution-environment:
    container-engine: podman
    enabled: true
    image: quay.io/jerrycorum/demo_ee:1.2
    pull:
      policy: missing
  playbook-artifact:
    enable: False
  logging:
    level: critical
  mode: stdout